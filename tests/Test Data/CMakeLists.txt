set(TEST_DATA_DIRECTORY "${CMAKE_RUNTIME_OUTPUT_DIRECTORY}/Test Data/")
target_compile_definitions(${TESTS_NAME} PRIVATE TEST_DATA_DIRECTORY="${TEST_DATA_DIRECTORY}")

file(COPY "${CMAKE_CURRENT_SOURCE_DIR}/a.txt"
        DESTINATION TEST_DATA_DIRECTORY)
file(COPY "${CMAKE_CURRENT_SOURCE_DIR}/b.txt"
        DESTINATION TEST_DATA_DIRECTORY)
file(COPY "${CMAKE_CURRENT_SOURCE_DIR}/c.txt"
        DESTINATION TEST_DATA_DIRECTORY)
file(COPY "${CMAKE_CURRENT_SOURCE_DIR}/d.txt"
        DESTINATION TEST_DATA_DIRECTORY)
